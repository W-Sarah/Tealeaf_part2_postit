<p><%= render @post.categories %></p>
<% if logged_in? and (current_user.id == @post.user_id or current_user.admin?) %>
<p class='edit'><%= link_to 'Edit this post', edit_post_path(@post) %></p>
<% end %>
<%= render 'shared/content_title', title: @post.title %>



<p><%= @post.description %>
<hr>
</p>

<% if logged_in? %>
<p>Leave a comment
  <%= render 'shared/form_error', form: @comment %>
  <%= form_for [@post, @comment] do |f| %>
  <%= f.text_area :body, rows: 5%>
  </br>
  <%= f.submit 'Submit comment', class: 'btn btn-primary' %>
  <% end %>
<% end %>
</p>

<hr>
<h4>Comments</h4>
<%= render 'comments/comments', object: @post %>




